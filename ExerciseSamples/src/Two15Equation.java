public class Two15Equation {
    public static void main(String[] args) {
        double x1=0, y1=0, x2=0, y2 =0;

        double result = Math.sqrt(
                Math.pow(x2-x1,2)  + Math.pow(y2-y1,2)
        );
        double samething = Math.sqrt((x2-x1)*(x2-x1) + (y2-y1)*(y2-y1));
        String dfdf = "";
    }
}
